"use client";
import { FC } from "react";

const ProfileUserInfo: FC = () => {
    return (
        <div>
            <h1>ProfileUserInfo</h1>
        </div>
    );
};

export default ProfileUserInfo;
